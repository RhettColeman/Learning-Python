username = input('Username: ')
password = input('Password: ')

passlen = len(password)
passhid = password.replace(password,'*' * len(password))

print('*' * 20)
print(f'Hello {username} your password {passhid} is {passlen} letters long')
print('*' * 20)
